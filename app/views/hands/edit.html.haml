.row
  .col-md-4.col-md-offset-4

    %h1 Редактирай ръка
    - @hand.name
    .well
      = simple_form_for(@hand, html: { class: 'form-horizontal form-new-edit' }, url: {action: "update", id: @hand.id}, method: `post`) do |f|
        %fieldset
          = f.input :name
          = f.input :priority
          = f.association :position
          = f.association :blind
                   
          
          %p.buttons
            = f.button :submit, class: "btn btn-primary"
            = link_to "Back", {action: "index"}, class: "btn btn-default"