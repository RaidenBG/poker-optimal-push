.row
  .col-md-4.col-md-offset-4

    %h1 Редактирай позиция
    - @position.name
    .well
      = simple_form_for(@position, html: { class: 'form-horizontal form-new-edit' }, url: {action: "update", id: @position.id}, method: `post`) do |f|
        %fieldset
          = f.input :name
          = f.input :priority
                          
          
          %p.buttons
            = f.button :submit, class: "btn btn-primary"
            = link_to "Back", {action: "index"}, class: "btn btn-default"